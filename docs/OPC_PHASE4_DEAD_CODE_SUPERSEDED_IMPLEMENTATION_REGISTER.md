# OPC Phase 4 Dead-Code / Superseded-Implementation Register

**Rule:** unused now ≠ proven dead. No code was removed.

| Candidate / area | Observed status | Classification | Evidence checked | Decision / next gate |
|---|---|---|---|---|
| `lib/core/utils/export_utils_replacement.dart` | One-line tracked placeholder says active implementation is `export_utils.dart` | **PROVEN DEAD** | Repository-wide source/test/script/packaging/docs search found no reference; no dynamic registration; Git history is baseline introduction; all consumers import `export_utils.dart`; no schema/JSON/restore/platform role | Removal is safe only as a later authorized documentation/repository cleanup; not removed in Phase 4 |
| `lib/features/predmeti/reminders/reminder_mvp_service.dart` | Older-looking MVP name; actively constructed in `app.dart` and `lista_predmeta_screen.dart` | ACTIVE — TARGET FIT / REFACTOR CANDIDATE | Direct production references and Windows reminder task evidence | Do not remove until derived-state and notification migration is complete |
| `lib/features/predmeti/statistika_v1/**` | Versioned name suggests supersession | ACTIVE — REFACTOR CANDIDATE | `izvestaji_screen.dart` directly constructs `StatistikaSnapshotService`; aggregator consumes it | Preserve until reports contract is mapped |
| `core_v2/scenario/**` legacy IDs/adapters/helpers | Contains `legacy` branches and unused-element annotations | COMPATIBILITY / MIGRATION INFRASTRUCTURE | Scenario persistence/transfer/runtime tests, legacy fixtures, user-defined data repair and locked contract | No removal without SCENARIO unlock and persisted-format proof |
| `predmeti_table.dart` legacy fields | Compatibility-derived fields such as legacy `radniStatus` | COMPATIBILITY / MIGRATION INFRASTRUCTURE | Current repository/UI fallback and migration history; tests cover legacy behavior | Retain until supported upgrade/data audit proves safe |
| `core/catalog/stock_catalog_identity.dart` legacy stable IDs | Maps historical SANDUK identifiers | COMPATIBILITY / MIGRATION INFRASTRUCTURE | Stock tests assert legacy IDs and canonicalization behavior | Retain; never remove by name/age |
| `core/json_transfer` legacy `OPC_BELEZNICA` adapter | Old single-PREDMET transfer format reader | COMPATIBILITY / MIGRATION INFRASTRUCTURE | Explicit parser flag, tests and import entry points | Retain until supported import policy changes with owner decision |
| `database.dart` older migration branches | Historical schema checkpoints 1–26 | COMPATIBILITY / MIGRATION INFRASTRUCTURE | `schemaVersion=27`, onUpgrade gates, v19–v26 recovery tests, package downgrade tests | Retain; only split physically later |
| `schema_recovery.dart` additive recovery | May look like duplicate migration repair | COMPATIBILITY / MIGRATION INFRASTRUCTURE | Before-open recovery, fail-closed mismatch contract, canonical migration tests | Retain and characterize; not dead |
| `database.g.dart` generated Drift code | Very large generated file | COMPATIBILITY / MIGRATION INFRASTRUCTURE | Generated by Drift and consumed by database/repositories | Never classify as ordinary dead code; regenerate only through build tooling |
| entitlement/license payload readers | Product restriction wording changed over time | COMPATIBILITY / MIGRATION INFRASTRUCTURE | Active settings/stock policy imports, parser tests, installation binding | Preserve payload compatibility; owner controls future distribution policy |
| `scenario_module_screen.dart` private unused helpers | `ignore: unused_element` and legacy serialized-condition helpers | UNRESOLVED / REQUIRES MIGRATION AUDIT | Comments explicitly retain legacy reads; no complete dynamic/persisted format audit | Do not classify dead |
| Old document/format helpers under `core/utils` | Shared names and mixed callers | UNRESOLVED / REQUIRES MIGRATION AUDIT | Active importers and Android storage contract test; role split not yet characterized | Move/refactor only after contract inventory |

## Proven-dead count

`PROVEN DEAD = 1`. No other source path has complete evidence for removal. In particular, compatibility infrastructure is not downgraded to dead code because a current UI path no longer calls it.

