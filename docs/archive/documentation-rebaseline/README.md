# Archived / Superseded Documentation Boundary

This directory is outside the active current documentation path. Material placed here is preserved for provenance only and must not be read as current product, business, implementation, runtime or release authority.

The current navigation root is `../../current/OPC_AUTHORITY_MANIFEST.md`.

`legacy-homes/` contains the pre-cutover snapshots of the former five-home
documents. `podsetnik-lineage/` contains retired PODSETNIK task/pseudocode
surfaces. Both archive branches are provenance only and are excluded from the
current authority path.

Direct OWNER decisions retain their authority only when identifiable as direct decisions and applicable to the current scope; historical task naming, reports and internal pseudocode do not become authority by appearing in this archive.
