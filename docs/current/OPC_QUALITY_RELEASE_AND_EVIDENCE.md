# OPC Quality, Release and Evidence Boundary

**Status:** `CURRENT CONTROL SURFACE — THIS TASK IS DOCUMENTATION-ONLY`

The normal code-task sequence remains targeted tests when relevant, conclusive `flutter analyze`, full `flutter test`, authorized platform build, then separately identified runtime acceptance. This documentation cutover does not rerun QA/build and does not convert prior evidence into current acceptance.

Evidence must identify the exact executable, companion `data/app.so`, timestamps, SHA-256 values, database path/hash, platform, user action and observed result. A build name or folder alone is insufficient. Runtime proves observation; source/tests prove technical behavior; neither proves owner business policy.

The corrected Windows build candidate is recorded with:

- `OPC.exe` SHA-256 `99ED7DB0A07DF74D6249B088DD0B3D63777A6DDE97CF2C14ABF83A107CBEB5E6`;
- `data/app.so` SHA-256 `7548E1B1C02B5F6EF11525BE2510F01D08A2C70C249408AD8725AD477A44666B`.

The canonical DB hash drift after prior runtime is recorded as an observed runtime data-state change. Under the latest OWNER clarification, normal runtime database writes are not by themselves a migration/schema defect. This documentation task does not perform any additional DB operation or claim release/publication acceptance.
