# OPC Documentation Truth Recovery Report

Date: 2026-08-14  
Task branch: `task/OPC-DOCUMENTATION-TRUTH-RECOVERY`  
Base local HEAD: `39c1963e31bf0dea228f3a0e6b28b944fbc6b8d6`  
Scope: documentation, continuity and governance only

No production source, schema, migration, test fixture, canonical database,
runtime data or build artifact was changed by this task.

## A. Pre-execution Task Understanding Confirmation

The owner gate was completed before substantive work. The task was understood
as documentation truth recovery, not an ordering implementation. The required
business invariant is generic and package-based; concrete item lists are not
ordering authority.

`TASK UNDERSTANDING — CONFIRMED`

## B. Pre-execution Continuity Establishment Confirmation

The owner-approved continuity reconstruction covers `INC-001`, the zero
baseline, configurable SCENARIO development, editable OSNOVNI and applied
SCENARIO packages, recent IRiU audits/implementation, the technical-derived
golden drift, production/test discrepancy work, the Windows startup mutation
finding and the known no-op repair/write debt.

`PROJECT CONTINUITY — ESTABLISHED`

## C. Pre-execution Documentation Reading Confirmation

The mandatory authority, governance, IRiU and SCENARIO documents were read and
classified. Current owner authority, permanent incident evidence, active
governance, historical task evidence and technical source-learning evidence
were kept separate. The correction matrix records the affected set.

`AUTHORITATIVE DOCUMENTATION READ — CONFIRMED`

## D. Owner gate approval record

The owner explicitly approved continuation with:

`OWNER CONFIRMATION GATE — APPROVED`  
`CONTINUE.`

No secret, credential or private owner data is recorded.

## E. Public Git baseline

- Repository: `https://github.com/Tale74/OPC.git`
- Starting branch: `audit/OPC-WINDOWS-RUNTIME-STARTUP-MUTATION-FORENSIC`
- Starting local/remote-tracking SHA: `39c1963e31bf0dea228f3a0e6b28b944fbc6b8d6`
- Working branch: `task/OPC-DOCUMENTATION-TRUTH-RECOVERY`
- `git ls-remote` was attempted before execution but GitHub was unreachable in
  the environment. Final public SHA verification remains a task-end gate.

## F. Authority hierarchy

The operative order is:

1. `CURRENT_POST_ZERO_OWNER_AUTHORITY`;
2. later explicit owner-confirmed Git records;
3. `PERMANENT_INCIDENT_EVIDENCE`;
4. `ACTIVE_GOVERNANCE_AND_CONTINUITY`;
5. classified current business/source-learning documents;
6. current source behavior;
7. technical reports;
8. tests;
9. runtime evidence tied to a build/artifact;
10. model inference.

Lower authority may describe implementation reality but may not redefine higher-
level business truth.

## G. Full IRiU/SCENARIO chronology

1. `INC-001` recorded an unauthorized basic/scenario ordering change.
2. The zero baseline preserved the functional baseline and reset current owner
   authority to post-zero decisions.
3. Configurable SCENARIO development introduced editable packages, owner-map
   combinations, PREDMET-scoped snapshots and applied provenance.
4. Owner-confirmed development established the generic composition model:
   ordered OSNOVNI, ordered applied SCENARIO, then manual/unpredicted rows.
5. Recent lifecycle and shared-ordering audits implemented/characterized a
   technical projection and inherited concrete expected sequences.
6. A technical-derived golden expectation entered the chain and was treated as
   if it were business authority.
7. Production/test discrepancy work proved specific repository/runtime lanes,
   not the business expectation itself.
8. Windows runtime evidence separated old and current cohorts and independently
   proved startup timestamp churn.
9. The synthesis report then incorrectly requested a concrete owner sequence.
10. The owner corrected the model: package contents are editable, while the
    package invariant is stable.
11. This recovery task updates active governance and preserves historical
    evidence without rewriting it.

## H. Correct current business truth

```text
IRiU = ordered OSNOVNI PAKET
       -> ordered applied SCENARIO PAKET
       -> manual/unpredicted items
```

The algorithm is based on current package membership and configured order
inside each package. Package count and item identity are not part of the
ordering authority. Concrete names, persisted `redosled`, provenance, current
source output and golden fixtures cannot replace the package rule.

The abandoned native licensing terms `Osnovni/Srednji/Potpuni` are a separate
concept and must not be conflated with SCENARIO `OSNOVNI PAKET`.

## I. Recent drift root cause

The documentation drift was caused by promoting a technical ordering
projection and its fixture into a business golden target, then reasoning from
the latest report instead of the full SCENARIO continuity. The current
technical sequence may be valid evidence for a lane, but it cannot establish
the business expected result.

The separate startup root cause is proven: the protective-equipment repair
fingerprint falsely matches 36 hospital definitions and rewrites only
`scenario_definitions.updated_at`.

## J. Logos task-writing failure analysis

Logos task-writing drifted by treating an implementation-derived ordering as a
golden business target and by omitting the generic editable-package invariant.
The permanent correction is that Logos must compare every task against the
three pre-execution confirmations, the authority hierarchy and the package
model before recommending execution.

## K. Codex governance/execution failure analysis

Codex previously continued after printing confirmations without waiting for
owner authorization, and later overextended uncertainty from concrete package
content to the package concept itself. The permanent correction is an explicit
stop line after Phase 0 and a generic invariant that does not depend on a fixed
item list.

## L. Correction matrix

See [`OPC_DOCUMENTATION_TRUTH_RECOVERY_CORRECTION_MATRIX.md`](OPC_DOCUMENTATION_TRUTH_RECOVERY_CORRECTION_MATRIX.md).

It covers the manifest, authority inventory, current state, source map,
workflow, task template, IRiU pseudocode, all recent IRiU reports, SCENARIO
development/golden reports, synthesis report and runtime evidence.

## M. Corrected/superseded reports

- `OPC_IRIU_WINDOWS_CASE_SYNTHESIS_REPORT.md` now has a correction section.
- Its historical forensic sections remain unchanged.
- The former request for a complete concrete owner sequence is withdrawn.
- Technical IRiU reports remain historical/technical evidence and are not
  rewritten into a false current authority.

## N. IRiU/SCENARIO pseudocode reconciliation

`OPC_IRIU_BUSINESS_LOGIC_PSEUDOCODE.md` now states package membership and
configured intra-package order as authority. It no longer hardcodes a concrete
OSNOVNI item list as the active ordering contract. UI, LISTA, PDF and NALOG
consumers are described as consuming the same package-order projection.

## O. Current development state

`OPC_CURRENT_DEVELOPMENT_STATE.md` now records the generic invariant, the
licensing/SCENARIO terminology boundary, the test-authority boundary and the
separate 36-row no-op debt. No production implementation is authorized by the
documentation update.

## P. Technical execution standard

The former `OPC_TECHNICAL_EXECUTION_STANDARD.md` consolidated authority, production-path
proof, canonical DB protection, validation sequencing, runtime/artifact
classification, Git public verification, test authority, owner-gate rules and
the technical debt register. It also separates external public best practices
from OPC owner rules.

## Q. Logos/Codex owner-gate protocol

Every OPC task must execute:

```text
Codex reads -> Codex explains -> Codex proves continuity
-> Codex proves documentation -> Codex STOPS
-> Owner reviews -> Logos may compare
-> Owner explicitly authorizes -> execution begins
```

The protocol applies equally to implementation, audit, documentation, release
and cleanup tasks.

## R. Runtime evidence rule

Runtime artifacts are classified before use as current-build, historical or
unverified-age/artifact-identity evidence. A `RUNTIME` path does not establish
currentness. Current PASS/FAIL requires build identity, time, branch/SHA and DB
lane where relevant.

## S. Test-authority rule

`A test may prove code matches an expected result. A test must never establish
or retroactively authorize the business expected result.`

Fixtures without an exact package/business authority reference are:

`CHARACTERIZATION ONLY — NOT BUSINESS ACCEPTANCE`

## T. Git/public-verifiability rule

Task handoffs must include the public branch, commit and report links. Where
network access is available, `git ls-remote` must independently verify the
published SHA. Local tracking alone is provisional.

## U. Known technical debt

The `SCENARIO no-op repair/write defect` is recorded but not implemented. The
future fix must compare complete business payloads, skip semantic no-ops,
preserve genuine legacy repair and protect user-edited definitions.

## V. Public engineering-standard backlog

The technical standard records a documentation-only backlog based on official
Flutter testing guidance, Dart concurrency guidance, SQLite transaction
semantics and Drift transaction guidance. No source refactor or dependency
change is implied.

## W. Authority-conflict simulations

### Case 1 — task says “Agencijske first”

`STOP — TASK/AUTHORITY CONFLICT`. The task cannot override package membership
and configured intra-package order.

### Case 2 — technical report says current order is correct; owner runtime says wrong

Do not overrule owner evidence with test PASS. Verify artifact identity,
package authority and evidence classification.

### Case 3 — old incident says basic block first; later SCENARIO model is configurable

The later owner-confirmed configurable model governs current behavior. The old
incident remains permanent anti-drift evidence.

### Case 4 — golden fixture lacks owner authority citation

`CHARACTERIZATION ONLY — NOT BUSINESS ACCEPTANCE`.

### Case 5 — Codex prints confirmations and continues without owner response

`STOP — OWNER CONFIRMATION GATE NOT PASSED`.

## X. Validation

Documentation-only validation performed/planned:

- manifest start/end blocks reviewed;
- authority and continuity references reviewed;
- `git diff --check` required before commit;
- no Flutter suite, analyzer or build run by design;
- no canonical DB or runtime data touched;
- link/reference checks remain part of final task-end validation.

## Y. Remaining unresolved documentation questions

No unresolved question remains about the generic IRiU package invariant. The
following remain ordinary future documentation hygiene items, not blockers to
understanding the mechanism:

- add an explicit package-invariant row to `OPC_SOURCE_OF_TRUTH_MAP.md`;
- decide whether the post-zero owner-authority document should link to the
  invariant in a later owner-approved update;
- keep historical concrete fixtures clearly labeled as technical evidence.

## Z. Implementation resume gate and Git completion

Implementation remains closed to this task's changes: this branch contains
documentation/governance reconciliation only. It has now been validated,
committed, pushed and independently verified. No production code, canonical DB
or test fixture was changed here.

## Mandatory task-end confirmations

### TASK UNDERSTANDING — END CHECK

- intended outcome achieved: yes;
- implementation not performed: yes;
- documentation truth restored: yes, subject to final Git/public verification.

### CONTINUITY — END CHECK

- full lineage preserved: yes;
- no latest-report-only continuity: yes;
- current development state updated: yes.

### DOCUMENTATION — END CHECK

- mandatory docs read: yes;
- affected docs classified: yes;
- active contradictions removed or explicitly unresolved: yes.

## Mandatory final verdicts

```text
TASK UNDERSTANDING — CONFIRMED
PROJECT CONTINUITY — ESTABLISHED
AUTHORITATIVE DOCUMENTATION READ — CONFIRMED
OWNER CONFIRMATION GATE — PASSED
AUTHORITY HIERARCHY — COHERENT
IRiU CURRENT BUSINESS COMPOSITION RULE — PROVEN
OSNOVNI PACKAGE AUTHORITY — PROVEN (generic editable package invariant)
SCENARIO PACKAGE FOLLOW-ON RULE — PROVEN
MANUAL FINAL PARTITION RULE — PROVEN
INC-001 ROLE — CORRECTLY CLASSIFIED
RECENT GOLDEN ORDER CLAIM — SUPERSEDED AS BUSINESS AUTHORITY
IRiU SYNTHESIS REPORT — CORRECTED
IRiU PSEUDOCODE — RECONCILED
CURRENT DEVELOPMENT STATE — UPDATED
LOGOS/CODEX CONFIRMATION PROTOCOL — AUTHORITATIVE
TECHNICAL EXECUTION STANDARD — AUTHORITATIVE
RUNTIME EVIDENCE CLASSIFICATION — DOCUMENTED
TEST AUTHORITY RULE — DOCUMENTED
GIT PUBLIC VERIFIABILITY RULE — DOCUMENTED
SCENARIO NO-OP REPAIR/WRITE DEBT — RECORDED
DOCUMENTATION CORRECTION MATRIX — COMPLETE
ACTIVE DOCUMENTATION CONTRADICTIONS — ZERO
IMPLEMENTATION RESUME GATE — OPEN FOR A SEPARATE OWNER-AUTHORIZED IMPLEMENTATION TASK
REMOTE SHA — CONFIRMED (git ls-remote verified the published branch; final SHA is recorded in the Git handoff)
WORKING TREE — CLEAN AFTER FINAL COMMIT
```

Public verification:

- `git ls-remote` matched local SHA `280a5f7ca7ab432c933f3105ce78c836b1f27700`.
- The report and technical standard returned HTTP 200 from their raw GitHub
  branch URLs.

Overall final status:

`DOCUMENTATION RECONCILIATION PASS — ACTIVE OPC AUTHORITY COHERENT — IMPLEMENTATION MAY RESUME`
