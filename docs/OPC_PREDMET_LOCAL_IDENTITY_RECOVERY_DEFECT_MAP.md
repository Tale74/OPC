# PREDMET Local-Identity / Recovery Acceptance — Pre-Implementation Defect Map

This task-local matrix records only source-confirmed risks admitted to the bounded implementation pass. It does not introduce a new identity model, owner policy, or successor program.

Current disposition: bounded local rebinding, deletion-reference coverage,
collision-safe creation and four-state full-backup identity preflight are
implemented. Case 2 is deliberately fail-closed and remains out of scope for
merge/reconciliation. Focused acceptance proof is in
`OPC_PREDMET_LOCAL_IDENTITY_RECOVERY_ACCEPTANCE_MATRIX.md`. The full Flutter
suite completed with 443 PASS and 10 expected skips; release-build gates also
passed serially. Case 2 successor: `FULL-BACKUP MISSING/INCOMPLETE FIRMA
IDENTITY — USER FALLBACK + SAFE MERGE/RECONCILIATION DESIGN AND ACCEPTANCE`.

| Surface | Current behavior | Proven risk | Required result | Existing mechanism reusable? | Source change required? | Test proof |
|---|---|---|---|---|---|---|
| New individual import user metadata | `_uvoziPredmetUBazu` inserts the transfer `PredmetiData` companion unchanged; the UI carries the session actor only for replacement. | Source-database `savetnikId`, `createdByKorisnikId` and `lastBusinessModifiedByKorisnikId` can become destination-local authority by numeric collision or absent-id coincidence. | Require an active destination-local actor; bind adviser, creator and last modifier to that local actor while preserving imported business truth/version. | Existing `SessionService` actor hand-off and repository transaction. | Yes — transfer normalization and import entry point. | Foreign equal-id, absent-id, inactive/missing actor and rollback cases. |
| Same-identity replacement metadata | Replacement calls `PredmetiRepository.zameniPredmetSaPovezanimPodacima` with the imported row and replaces all columns. | Imported adviser/creator/modifier IDs can overwrite legitimate destination-local ownership/history. | Preserve destination adviser/creator; set only `lastBusinessModifiedByKorisnikId` to the active local replacement actor; retain `IMPORT_REPLACE` and local history. | Existing stable local id, transaction and `logIzmena` event. | Yes — repository replacement projection. | Metadata preservation plus `IMPORT_REPLACE` actor test. |
| Missing/inactive actor | Replacement rejects only a null UI actor; new import has no actor gate; a stale/inactive session id is not re-read. | Business state can be created or replaced without legitimate active local authority. | Resolve actor from the destination database and fail closed before mutation unless active and an existing permitted role. | Existing `Korisnici.aktivan`, role values and session id. | Yes — shared actor resolver. | Active ADMINISTRATOR/SAVETNIK, inactive, missing and absent actor tests. |
| Permanent-user deletion references | `korisnikReferenceSummary` counts only `Predmeti.savetnikId` and `logIzmena.korisnikId`. | Creator-only or last-modifier-only attribution can be deleted, destroying historical provenance. | Count adviser, creator, last modifier and audit references; retain deactivation/role-change semantics. | Existing deletion guard and summary object. | Yes — database reference summary. | Creator-only and last-modifier-only deletion-block tests. |
| Full-backup PIB/MB preflight | Structural and identity preflight now runs before destructive confirmation/write. Matching identity proceeds; mismatch blocks; fresh local state can establish complete identity. | Missing/incomplete backup identity must never be treated as a match when local business state exists. | Fail closed for Case 2 before destructive write and preserve local state; leave merge/reconciliation to the explicit successor. | Existing backup validators and `_validateBackupFirmIdentityPreflight`. | Yes — bounded pre-confirmation validation entry point. | Matching, mismatch, malformed, Case-2 missing/blank identity, and Case-3 fresh complete-identity tests. |
| Same-minute `brojPredmeta` creation | Candidate is exactly the minute-based formatter and is inserted without collision check. | Two local creations in one minute can receive the same business number. | Resolve a deterministic suffix within the same local FIRMA/database inside the write transaction; never ask the user to resolve a technical collision. | Existing formatter, local database transaction and current firm-scoped key. | Yes — repository candidate allocation. | Two simultaneous/logically same-minute creations and legacy duplicates. |
| Conflict UI identity display | Conflict dialog displays source-local adviser/modifier numeric fields as ordinary metadata. | Numeric local IDs can be read as authoritative cross-database identity. | Preserve cancel/keep/replace and multiple-match fail-closed behavior; label local IDs as local metadata or omit them from authoritative identity presentation. | Existing dialog and conflict query. | Yes — narrow presentation wording only if needed. | Existing one-match/multiple-match conflict tests plus display assertion if changed. |
| `IMPORT_NEW` scope | No local `IMPORT_NEW` audit event is written for a new individual import. | New-import creator attribution is corrected, but event taxonomy could be broadened without an accepted audit policy. | Keep new-import event logging separate unless source evidence proves it is required by this task; document classification. | Existing bounded `IMPORT_REPLACE` event only. | No event-scope change unless source proves direct necessity. | Explicit scope regression and no-unrequested-event assertion. |
| Rollback boundaries | Individual import/replacement and backup restore use transactions, but actor/preflight gates are incomplete. | A rejected actor, malformed backup or collision failure could occur after business mutation begins. | Perform all in-scope gates before mutation and preserve existing replacement/RR-008 rollback guarantees. | Existing Drift transactions and restore coordinator. | Yes — gate placement and collision allocation. | Invalid JSON, actor failure, preflight failure and collision failure leave state unchanged. |
